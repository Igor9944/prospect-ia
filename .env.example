# Optional. Without it, the local API uses built-in heuristics.
GEMINI_API_KEY=

# Default 3001. The GitHub Pages frontend talks to this process on your machine.
PORT=3001

# Comma-separated extra origins if you host the UI somewhere other than github.io
# CORS_ORIGINS=https://igor9944.github.io
